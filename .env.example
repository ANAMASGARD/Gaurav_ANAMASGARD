# Netlify's Project ID is not secret. Copy this file to .env after linking.
NETLIFY_SITE_ID=

# Do not save the OAuth token created by `netlify login` in this repository.
# The CLI stores it in ~/.config/netlify/config.json.
